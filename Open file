 OpenFileDialog openfileDialog1 = new OpenFileDialog();
            if (openfileDialog1.ShowDialog() == DialogResult.OK) // im gonna make it so the script goes in the thing soon i can't work full time on scripts for roblox.
